package auth

// Package auth is the future module boundary for registration, login, sessions,
// token issuing, and subscription-aware access checks. Current behavior remains
// in the legacy user usecase while the refactor is migrated gradually.
